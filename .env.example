# The frontend talks to the Express backend via the /api proxy in dev
# (see vite.config.ts). Set this only for a production build pointed at a
# separately-hosted backend.
# VITE_API_URL=https://your-deployed-backend.example.com/api
